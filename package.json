{
  "name": "xmess",
  "version": "2.0.0",
  "dependencies": {
    "lerna": "3.14.1",
    "typescript": "3.4.5"
  },
  "devDependencies": {
    "@types/jest": "24.0.13",
    "jest": "24.8.0",
    "ts-jest": "24.0.2",
    "tslint": "5.16.0",
    "tslint-config-airbnb-base": "0.3.0"
  },
  "scripts": {
    "lint": "tslint packages/**/*.ts",
    "lint:fix": "tslint packages/**/*.ts --fix",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "build": "lerna run build",
    "link": "lerna link",
    "publish": "lerna publish"
  }
}
